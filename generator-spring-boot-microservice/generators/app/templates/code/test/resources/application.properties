build.version=0.0.1-SNAPSHOT-TEST
spring.application.name=<%= application.name %>
application.env=test
functionaltest.applicationloadbalancer=http://localhost

#Extra configurations isolated
spring.profiles.include=kafka
